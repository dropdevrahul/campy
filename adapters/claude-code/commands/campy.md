---
description: Show your campy pet's current status
---

Show the pet's current mood, happiness, and speech.

```bash
ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
bun "$ROOT/cli/campy.ts" status
```
